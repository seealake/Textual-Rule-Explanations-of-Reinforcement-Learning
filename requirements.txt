# Base dependencies for Linux/Windows local and remote servers.
# Torch GPU wheels are handled in setup.sh/setup.bat with fallback logic.

# --- RL environments and training stack ---
gymnasium[box2d]>=1.0.0
stable-baselines3>=2.3.0
highway-env>=1.9.0
minigrid>=2.3.0

# --- Scientific computing ---
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# --- ML / explainers ---
scikit-learn>=1.3.0
kneed>=0.8.5

# --- Visualization ---
matplotlib>=3.7.0
seaborn>=0.13.0

# --- Config / validation ---
pyyaml>=6.0
jsonschema>=4.18.0

# --- Logging / progress ---
tqdm>=4.65.0
rich>=13.7.0
tensorboard>=2.14.0

# --- Utility runtime deps sometimes required by Gym wrappers ---
pygame>=2.5.0

# --- Optional notebook tooling ---
# jupyter>=1.0.0
# notebook>=7.0.0
